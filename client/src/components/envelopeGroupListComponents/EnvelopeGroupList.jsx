import React from 'react';
import EnvelopeList from './EnvelopeList';

// Dynamically renders all groups in the current BudgetMonth > Budget > User.
export default function EnvelopeGroupList() {
  return(
    <div>
      <EnvelopeList />
    </div>
  );
}